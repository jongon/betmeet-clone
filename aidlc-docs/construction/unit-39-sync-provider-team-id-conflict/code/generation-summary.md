# Unit 39 Code Generation Summary

**Unit**: Sync — unique constraint conflict en `Team.providerTeamId`
**Stage**: Code Generation / Build and Test
**Timestamp**: 2026-06-17T21:48:25Z

## Outcome

No application code changes were required. The approved fix is schema-only:

- `prisma/schema.prisma`: removed `@unique` from `Team.providerTeamId`.
- `prisma/migrations/20260617180000_drop_provider_team_id_unique/migration.sql`: drops the unique index `teams_provider_team_id_key` if it exists.

The sync code remains unchanged because team identity is already resolved by `fifaCode`; `providerTeamId` is external-provider metadata.

## Verification

- `pnpm exec tsc --noEmit` — pass after removing stale local `.next/dev/types` artifacts generated by the dev server.
- `pnpm test` — pass, 265/265 tests.
- `pnpm build` — pass, includes Prisma Client generation and Next.js production TypeScript validation.

## Operations Gate

Production still requires:

1. Run `pnpm prisma:migrate` / `prisma migrate deploy` against the production database.
2. Run a smoke sync from `/admin` with scope `FULL`.
3. Confirm the sync run ends in `SUCCESS` and no longer raises `provider_team_id` unique-constraint errors.
